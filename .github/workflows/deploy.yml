name: Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy-dev:
    name: Deploy
    uses: ./.github/workflows/deploy-reusable.yml
    with:
      environment: prod
    secrets: inherit
