name: Deploy Release

on:
  release:
    types:
      - published

permissions:
  contents: read

jobs:
  prepare:
    name: Resolve Server Directory
    runs-on: ubuntu-latest
    outputs:
      release_minor: ${{ steps.resolve.outputs.release_minor }}
      server_dir: ${{ steps.resolve.outputs.server_dir }}
    steps:
      - name: Resolve Server Directory
        id: resolve
        shell: bash
        env:
          TAG: ${{ github.event.release.tag_name }}
        run: |
          if [[ ! "$TAG" =~ ^v([0-9]+)\.([0-9]+)\.([0-9]+)$ ]]; then
            echo "Expected SemVer Tag in format vMAJOR.MINOR.PATCH, got: $TAG" >&2
            exit 1
          fi
          echo "release_minor=${BASH_REMATCH[1]}.${BASH_REMATCH[2]}" >> "$GITHUB_OUTPUT"
          echo "server_dir=releases/v${BASH_REMATCH[1]}.${BASH_REMATCH[2]}/" >> "$GITHUB_OUTPUT"

  deploy-prod:
    name: Deploy Prod
    needs:
      - prepare
      - deploy-release
    uses: ./.github/workflows/deploy-reusable.yml
    with:
      deploy_files: html_skill
      environment: prod
      htaccess_mode: no-cache
      public_base_url_kind: releases
      public_base_url_path: /v${{ needs.prepare.outputs.release_minor }}
      server_dir: prod/
      skill_public_base_url_kind: prod
    secrets: inherit

  deploy-release:
    name: Deploy Release
    needs: prepare
    uses: ./.github/workflows/deploy-reusable.yml
    with:
      deploy_files: all
      environment: prod
      htaccess_mode: immutable
      public_base_url_kind: releases
      public_base_url_path: /v${{ needs.prepare.outputs.release_minor }}
      server_dir: ${{ needs.prepare.outputs.server_dir }}
    secrets: inherit
